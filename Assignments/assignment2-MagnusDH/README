# How to run code

    * Run sequential version
        - Navigate to the "code/serial" folder
        - run "make" to compile the program
        - run "./main" to run program
    
    * Run parellel version with MPI
        - Navigate to the "code/parallel" folder
        - run "make run_parallel" to compile and run the program

    * Run parellel version with MPI+openMP
        - Navigate to the "code/parallel" folder
        - run "make run_openMP" to compile and run the program
